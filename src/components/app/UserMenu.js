import React from "react";

export default function UserMenu() {
  return (
    <div>
      <img src={"/ "} alt={""} width="32"></img>
      <span>Welcome {}!</span>
      <button type="button" onClick={() => {}}>
        Logout
      </button>
    </div>
  );
}
